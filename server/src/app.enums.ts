export enum Evidence {
    Gunpowder = 0,
    Sperm = 1, 
    Saliva = 2, 
    Blood = 3,
    Fibers = 4,
    Fingerprints = 5,
}

export enum Role {
    Trainee = 0,
    Supervisor = 1
}

export enum Light {
    White = 0,
    Violet = 1,
    Blue = 2,
    BlueGreen = 3,
    Green = 4,
}

export enum Action {
    Found = 0,
    Dropped = 1,
    PickedUp = 2
}

export enum Filter { 
    GlassesBase = 0,
    Glasses435nm = 1,
    Glasses476nm = 2,
    Glasses529nm = 3,
    Glasses549nm = 4
}
