// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./databases/simulation_history.db"
}

// model Sim_history_record {
//   date          String
//   sim_id        String      @unique
//   events Sim_event[] 
// }

model Sim_event {
  event_id                 Int @id @unique @default(autoincrement())
  // sim_timestamp            String              
  user_id                  String             
  // action                   String
  // record                   Sim_history_record @relation(fields: [record_id], references: [sim_id])
  // record_id                String
}
