enum EventType{
    TAKE = "grabbed",
    DROP = "dropped",
    USE  = "uses"
}

enum GlassesType{

}

enum Roles{
    TRAINEE = "TRAINEE",
    SUPERVISOR = "SUPERVISOR"
}

export {EventType, GlassesType, Roles}