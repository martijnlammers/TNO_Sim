enum EventType{
    TAKE = "grabbed",
    DROP = "dropped",
    USE  = "uses"
}

enum GlassesType{

}

enum Roles{
    TRAINEE = "TRAINEE",
    SUPERVISOR = "SUPERVISOR"
}

enum EvidenceTypes{
    BLOOD = "BLOOD",
    GUNPOWDER = "GUNPOWDER",
    BODILYFLUID = "BODILYFLUID",
    FIBER = "FIBER"
}


export {EventType, GlassesType, Roles, EvidenceTypes}